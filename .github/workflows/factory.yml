name: Rule Factory (Sing-box & Mihomo)

# ===============================
# 触发条件
# ===============================
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - '.github/workflows/factory.yml'

permissions:
  contents: write

jobs:
  produce-rules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # 1. 安装机器：sing-box 和 mihomo (最终修复 404)
      - name: Install Dependencies
        run: |
          # --- A. 安装 sing-box ---
          echo "--- Installing sing-box ---"
          S_VERSION=$(curl -s https://api.github.com/repos/SagerNet/sing-box/releases/latest | jq -r .tag_name)
          S_VERSION_NUM=${S_VERSION#v}
          wget --tries=5 "https://github.com/SagerNet/sing-box/releases/download/${S_VERSION}/sing-box-${S_VERSION_NUM}-linux-amd64.tar.gz"
          tar -zxvf "sing-box-${S_VERSION_NUM}-linux-amd64.tar.gz"
          sudo mv sing-box-*/sing-box /usr/local/bin/
          sudo chmod +x /usr/local/bin/sing-box
          sing-box version
          
          # --- B. 安装 mihomo (修复 404 错误: 使用更稳定的 Bash 查找链接) ---
          echo "--- Installing mihomo ---"
          M_VERSION=$(curl -s https://api.github.com/repos/MetaCubeX/mihomo/releases/latest | jq -r .tag_name)
          
          echo "Searching Mihomo download link for ${M_VERSION}..."
          
          # 关键修复：使用 curl + grep + sed 替代复杂的 jq 查找，保证兼容性
          M_URL=$(curl -s "https://api.github.com/repos/MetaCubeX/mihomo/releases/tags/${M_VERSION}" | \
                  grep -oP '"browser_download_url": "\K[^"]*mihomo-linux-amd64[^"]*\.gz"' | \
                  head -n 1)

          if [ -z "$M_URL" ]; then
            echo "Error: Could not find Mihomo download URL for linux-amd64."
            exit 1
          fi
          
          # 注意：M_URL 已经被双引号包裹了，所以我们需要将其去掉
          M_URL=${M_URL//\"/}

          wget --tries=5 "$M_URL" -O mihomo-download.gz
          gunzip mihomo-download.gz

          # 查找解压后的文件，并赋予执行权限
          M_EXECUTABLE=$(find . -maxdepth 1 -type f -name "mihomo-linux-amd64*" ! -name "*.gz" | head -n 1)

          if [ -z "$M_EXECUTABLE" ]; then
             M_EXECUTABLE=$(find . -maxdepth 1 -type f ! -name "*.gz" ! -name "mihomo-download" | head -n 1)
          fi

          if [ -z "$M_EXECUTABLE" ]; then
             echo "Fatal Error: Could not find the mihomo executable after unzipping. Cannot continue."
             exit 1
          fi
          
          chmod +x "$M_EXECUTABLE"
          sudo mv "$M_EXECUTABLE" /usr/local/bin/mihomo
          mihomo -v

      # 2. 运行工厂生产线
      - name: Run Factory Line (Process All Rules)
        run: |
          SOURCE_DIR="src"
          OUTPUT_DIR="rule-set"
          mkdir -p "$OUTPUT_DIR"
          shopt -s nullglob
          
          # --- B. 远程进货清单 (Sing-box AdGuard 转换) ---
          # 格式: "上游链接 | 自定义文件名" (文件名不带后缀)
          declare -a REMOTE_FILES=(
            "https://adguardteam.github.io/AdGuardSDNSFilter/Filters/filter.txt | AdGuardSDNSFilter"
          )
          
          # ----------------------------------------------------------------
          # 1. Processing Remote AdGuard Rules (带重试)
          # ----------------------------------------------------------------
          echo ">>> 1. Processing Remote AdGuard Rules..."
          for rule in "${REMOTE_FILES[@]}"; do
            url="${rule%%|*}"
            name="${rule##*|}"
            url=$(echo "$url" | xargs)
            name=$(echo "$name" | xargs)

            echo "📥 Downloading: $name"
            # curl 带有重试机制
            curl --retry 5 --retry-delay 5 -L -s -o "temp_${name}.txt" "$url"

            if [ -f "temp_${name}.txt" ] && [ -s "temp_${name}.txt" ]; then
               sing-box rule-set convert --type adguard --output "${OUTPUT_DIR}/${name}.srs" "temp_${name}.txt"
               echo "✅ Success: ${name}.srs"
               rm "temp_${name}.txt"
            else
               echo "⚠️ Warning: Download or file is empty: $url"
            fi
            echo "---"
          done

          # ----------------------------------------------------------------
          # 2. Processing Local Rules (src 根目录)
          # ----------------------------------------------------------------
          echo ">>> 2. Processing Local JSON & YAML Rules..."
          if [ -d "$SOURCE_DIR" ]; then
            
            # A. 编译 Sing-box JSON (*.json -> .srs)
            for json_file in "$SOURCE_DIR"/*.json; do
              filename=$(basename "$json_file" .json)
              echo "🔨 Compiling Sing-box JSON: $filename"
              sing-box rule-set compile --output "${OUTPUT_DIR}/${filename}.srs" "$json_file"
            done

            # B. 编译 Mihomo YAML (*.yaml / *.yml -> .mrs)
            if command -v mihomo &> /dev/null; then
                for yaml_file in "$SOURCE_DIR"/*.yaml "$SOURCE_DIR"/*.yml; do
                  base_name=$(basename "$yaml_file" | sed -E 's/\.y(a)?ml$//') 
                  echo "⚙️ Converting Mihomo YAML: $base_name"
                  mihomo convert-ruleset domain yaml "$yaml_file" "${OUTPUT_DIR}/${base_name}.mrs"
                done
            fi
          fi

          # ----------------------------------------------------------------
          # 3. Processing Mihomo List Rules (按子文件夹区分类型)
          # ----------------------------------------------------------------
          
          # A. Domain 列表 (src/domain/*.list & *.txt -> .mrs)
          echo ">>> 3A. Processing Mihomo Domain Lists (src/domain)"
          if [ -d "$SOURCE_DIR/domain" ] && command -v mihomo &> /dev/null; then
            # 兼容 .list 和 .txt 两种后缀
            for list_file in "$SOURCE_DIR"/domain/*.list "$SOURCE_DIR"/domain/*.txt; do
              # 动态确定文件名 (移除 .list 或 .txt 后缀)
              filename=$(basename "$list_file")
              filename=${filename%.list}
              filename=${filename%.txt}

              echo "⚙️ Converting Domain LIST: $filename (Source: $list_file)"
              mihomo convert-ruleset domain text "$list_file" "${OUTPUT_DIR}/${filename}.mrs"
            done
          fi

          # B. IP-CIDR 列表 (src/ip/*.list & *.txt -> .mrs)
          echo ">>> 3B. Processing Mihomo IPCIDR Lists (src/ip)"
          if [ -d "$SOURCE_DIR/ip" ] && command -v mihomo &> /dev/null; then
            # 兼容 .list 和 .txt 两种后缀
            for list_file in "$SOURCE_DIR"/ip/*.list "$SOURCE_DIR"/ip/*.txt; do
              filename=$(basename "$list_file")
              filename=${filename%.list}
              filename=${filename%.txt}
              
              echo "⚙️ Converting IPCIDR LIST: $filename"
              mihomo convert-ruleset ipcidr text "$list_file" "${OUTPUT_DIR}/${filename}.mrs"
            done
          fi

      # 3. 提交成品到仓库 (修复 Git 报错 128)
      - name: Commit and Push changes
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          
          # 只有找到了文件才执行 git add
          find rule-set/ -name "*.srs" -o -name "*.mrs" | xargs git add
          
          # 提交 (如果没有变化则跳过)
          git commit -m "Factory output update [skip ci]" || exit 0
          git push
